# Release Output

CI writes production artifacts here:

- `package/` - staged install directory
- `Overlay.zip` - launcher update package
- `Setup.exe` - Inno Setup bootstrap installer
- `latest.json` - remote version manifest

Do not commit generated release binaries.

